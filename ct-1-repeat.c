#include <stdio.h>
int main()
{
int n,temp, ------- , ------- ;
scanf("%d",&n);
temp=n;
while (-------) {
	int d = temp % 10;
	temp /= 10;
	if (------------------) 
{
			---------;
			product *= d;
		}
	}
	printf("Sum = %d ",sum);
	printf("Product = %d",product);
	return 0;
}

