#include <stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
    int a,b,______,p;
    printf("Enter a and b:");
    scanf("%d%d",&a,&b);
      p = ___(a*b);
        if (___)   
        return 1;
        while (____)   
    {
        count++;
        _____;
    }
    
      printf("\nCount=%d",count);
    return 0;
}